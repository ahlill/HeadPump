<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.tpusk.headpump">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_headpump"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_headpump_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity android:name="id.tpusk.headpump.ListDataActivity"
            android:parentActivityName="id.tpusk.headpump.MainActivity"
            android:screenOrientation="portrait"/>
        <activity android:name="id.tpusk.headpump.InfoActivity"
            android:parentActivityName="id.tpusk.headpump.MainActivity"
            android:screenOrientation="portrait"/>
        <activity android:name="id.tpusk.headpump.MainActivity"
            android:screenOrientation="portrait"/>
        <activity android:name="id.tpusk.headpump.SplashScreenActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>